{
  "version": 2,
  "installCommand": "echo skip",
  "buildCommand": "echo skip",
  "outputDirectory": "build/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
